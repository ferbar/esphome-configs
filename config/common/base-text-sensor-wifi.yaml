# text-sensor:
#   - !include common/base-text-sensor-wifi.yaml

platform: wifi_info
ip_address:
  name: "${devicename} IP"
  icon: mdi:ip-network-outline
mac_address:
  name: "${devicename} Mac"
  icon: mdi:network-outline
